# canary
A Ruby coding evaluation and RL environment. Ruby as a tail-generalization canary: corpus scarcity means models cannot memorize it, syntactic permissiveness means weak models fail silently rather than loudly.
