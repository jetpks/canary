# Copy to .env (gitignored) to let the opt-in live-provider tests run.
#
# Nothing reads this file unless CANARY_LIVE is set:
#
#     CANARY_LIVE=1 bundle exec rake test
#
# Without CANARY_LIVE the suite reads no credential and opens no socket.
ANTHROPIC_API_KEY=sk-ant-...
